import { useLocalSearchParams, useRouter } from "expo-router";
import { View, Text } from "react-native";

export default function PostPage() {


    return (
        <View>
            {/* <Text>{postObj.description}</Text>
            <Text>{postObj.image_url}</Text> */}
        </View>
    );
}